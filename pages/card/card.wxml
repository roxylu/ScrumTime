<view class="container">
  <view id="card" class="card" data-hi="WeChat" bindtap="tapCard">
    <text class="card__label" wx:if="{{front}}">你猜</text>
    <text class="card__label" wx:else>{{time}}</text>
  </view>
</view>