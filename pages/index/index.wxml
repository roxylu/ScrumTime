<view class="container">
  <view class="weui-grids">
    <view wx:for="{{times}}">
      <navigator class="weui-grid" url="../card/card?time={{item}}">
        <text class="weui-grid__label">{{item}}</text>
      </navigator>
    </view>
  </view>
</view>
