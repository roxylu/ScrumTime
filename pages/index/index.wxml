<view class="container">
  <view class="weui-grids">
    <view class="weui-grid" wx:for="{{times}}">
      <navigator url="../card/card?time={{item}}">
        <text class="weui-grid__label">{{item}}</text>
      </navigator>
    </view>
  </view>
</view>
